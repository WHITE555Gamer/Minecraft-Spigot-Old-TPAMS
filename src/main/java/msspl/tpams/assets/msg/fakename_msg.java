package msspl.tpams.assets.msg;

import static msspl.tpams.assets.info.info.*;
import static org.bukkit.ChatColor.*;

public class fakename_msg {

    public static String Feed_Msg(String s){

        return im_line_n + GREEN + "Command succeeded: Changed own name to " + s + "." + RESET + "\n" + im_line;

    }
}
